[project]
name = "py-smallbank"
version = "0.1.0"
description = "SmallBank OLTP Benchmark (Python port)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pymysql>=1.2.0",
    "psycopg2-binary>=2.9.0",
]
